<% provide(:title, @title) %>

<div class="row common-header">
  <div class="col-md-12">
    <a href="#">
      <%= link_to (fa_icon "angle-left"), '#', class: "fa-2x" %>
    </a>
    <p class="text-center">following</p>
  </div>
</div>

<table class="table common-list">
  <% if @users.any? %>
    <%= render @users %>
  <% end %>
</table>

<div class="row">
  <div class="col-xs-12">
    <%= will_paginate @users, :page_links => false, class: 'pagination-sm' %>
  </div>
</div>

<%#= link_to "view my profile", @user %>