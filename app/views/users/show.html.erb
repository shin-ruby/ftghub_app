<%= provide(:title, @user.name) %>

<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <%= image_tag("user.jpg", size: "80", alt: @user.name, class: "gravatar") %>
        <%= @user.name %>
      </h1>
    </section>
  </aside>
</div>
