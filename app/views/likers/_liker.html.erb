<%= form_for [@fightpost, @fightpost.likers.build], remote: true do |f| %>
	<%= button_tag(type: 'submit', class: "btn-link") do %>
	   <%= content_tag(:i, nil, class: "fa fa-heart heart-active") %><%= Liker.where(fightpost_id: @fightpost).count %>
	<% end %>
<% end %>
