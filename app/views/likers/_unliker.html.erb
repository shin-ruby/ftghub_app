<%= form_for [@fightpost, @fightpost.likers.find_by(user: current_user)],
						 html: { method: :delete }, remote: true do |f| %>
	<%= button_tag(type: 'submit', class: "btn-link") do %>
	   <%= content_tag(:i, nil, class: "fa fa-heart") %><%= Liker.where(fightpost_id: @fightpost).count %>
	<% end %>
<% end %>